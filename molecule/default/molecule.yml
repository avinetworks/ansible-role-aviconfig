---
dependency:
  name: galaxy
driver:
  name: docker
lint: |
  yamllint tasks -c molecule/default/.yamllint 
platforms:
  - name:  instances
    image: ubuntu:16.04
provisioner:
  name: ansible
  lint:
    name: ansible-lint
verifier:
  name: ansible
